@model IEnumerable<DebtCalculatorWebApp.Models.CreditCardLoan>

@{
    ViewData["Title"] = "Index";
}

<h2>Index</h2>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.CreditCardLoanAmount)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.CreditCardLoanTime)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.CreditcardLoanApr)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.RecordDate)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.CreditCardLoanAmount)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.CreditCardLoanTime)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.CreditcardLoanApr)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.RecordDate)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.CreditCardLoanId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.CreditCardLoanId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.CreditCardLoanId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
