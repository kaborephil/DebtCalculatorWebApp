@model IEnumerable<DebtCalculatorWebApp.Models.CreditCardLoan>

@{
    ViewData["Title"] = "Index";
}

<h2>Credit Card Loan Details</h2>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                Amount
            </th>
            <th>
                Time
            </th>
            <th>
                APR
            </th>
            <th>
                Record Date
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @item.CreditCardLoanAmount.ToString("C")
            </td>
            <td>
                @item.CreditCardLoanTime
            </td>
            <td>
                @item.CreditcardLoanApr %
            </td>
            <td>
                @item.RecordDate.ToShortDateString()
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.CreditCardLoanId">Edit</a> |
                <a asp-action="Delete" asp-route-id="@item.CreditCardLoanId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
